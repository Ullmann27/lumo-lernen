name: Quota Health Check

on:
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: |
          echo "Build runner ist verfuegbar"
          echo "Wenn dieser Job laeuft, ist das Quota OK"
          date
          uname -a
